#!/bin/sh

error() { notify-send " ERROR" "$1"; exit 1 ;}



outname=$(echo "$1" | sed 's|flac|mp3|g')
case "$1" in
	*flac) ffmpeg -i "$1" -ab 320k -map_metadata 0 -id3v2_version 3 "$outname";;
	*) error "Selected file is not flac.";;
esac

notify-send " Conversion Complete" "$outname"
