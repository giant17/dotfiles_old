#!/bin/sh

# Add item to inbox taskwarrior
error() { notify-send " ERROR" "$1"; exit 1;}
prompt() { printf "$1" | dmenu -i -p "$2" ;}

TODO=$(prompt "" "Add to inbox:")
[ -z "$TODO" ] && error "Empty"
task add tag:inbox $TODO
notify-send " Add to inbox" "$TODO" -t 1000




